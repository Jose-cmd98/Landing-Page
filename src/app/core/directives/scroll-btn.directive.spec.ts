import { ScrollBtnDirective } from './scroll-btn.directive';

describe('ScrollBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollBtnDirective();
    expect(directive).toBeTruthy();
  });
});
